This project demonstrates how to create a User Registration Form using HTML and PHP, and how to securely insert user–submitted data into a MySQL database using prepared statements.

This experiment fulfills the following requirements:

✔ HTML registration form

✔ PHP script that receives POST data

✔ Inserts data into a database table named users

✔ Uses prepared statements (SQL injection–safe)

✔ Provides success/error messages

✔ Ready for upload to GitHub

📁 Project Structure
/experiment-2-registration/
│
├── index.html          # Registration form
├── register.php        # PHP backend to insert data into DB
└── README.md           # Documentation
